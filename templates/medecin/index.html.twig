{% extends 'base.html.twig' %}

{% block title %}Medecin index{% endblock %}

{% block body %}
    <div class="container">
    </div>
    <div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Specialite</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for medecin in medecins %}
            <tr>
                <td>{{ medecin.nom }}</td>
                <td>{{ medecin.specialite }}</td>
                <td>{{ medecin.email }}</td>
                <td>
                    {{ include('medecin/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6"></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('medecin_new') }}"><button type="button" class="btn btn-dark btn-sm">Create new</button></a>
    </div>
{% endblock %}
