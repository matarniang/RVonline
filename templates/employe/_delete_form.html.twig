<form method="post" action="{{ path('employe_delete', {'idemploye': employe.idemploye}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.idemploye) }}">
    <button class="btn">Delete</button>
</form>
