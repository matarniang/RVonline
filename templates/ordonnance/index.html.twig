{% extends 'base.html.twig' %}

{% block title %}Ordonnance index{% endblock %}

{% block body %}
    <h1>Ordonnance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idordonnance</th>
                <th>Medicament</th>
                <th>Idtraitement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ordonnance in ordonnances %}
            <tr>
                <td>{{ ordonnance.idordonnance }}</td>
                <td>{{ ordonnance.medicament }}</td>
                <td>{{ ordonnance.idtraitement }}</td>
                <td>
                    <a href="{{ path('ordonnance_show', {'idordonnance': ordonnance.idordonnance}) }}">show</a>
                    <a href="{{ path('ordonnance_edit', {'idordonnance': ordonnance.idordonnance}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ordonnance_new') }}">Create new</a>
{% endblock %}
