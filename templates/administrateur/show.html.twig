{% extends 'base.html.twig' %}

{% block title %}Administrateur{% endblock %}

{% block body %}
    <h1>Administrateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ administrateur.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ administrateur.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ administrateur.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administrateur_index') }}">back to list</a>

    <a href="{{ path('administrateur_edit', {'idadministrateur': administrateur.idadministrateur}) }}">edit</a>

    {{ include('administrateur/_delete_form.html.twig') }}
{% endblock %}
